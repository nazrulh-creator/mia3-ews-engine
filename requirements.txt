# MIA3 Early Warning Engine — runtime dependencies
# Core web + data stack (required)
fastapi==0.111.0
uvicorn[standard]==0.30.1
jinja2==3.1.4
python-multipart==0.0.9
SQLAlchemy==2.0.31
pydantic==2.7.4
pydantic-settings==2.3.4
pandas==2.2.2
numpy==1.26.4
passlib==1.7.4
itsdangerous==2.2.0
APScheduler==3.10.4
python-docx==1.1.2
Pillow==10.3.0

# Production database driver (sqlite is used as a zero-install local default)
psycopg2-binary==2.9.9

# ML stack — used when present, gracefully degraded when absent.
# Production serving of the back-tested model uses XGBoost + SHAP.
# These are heavier installs; the engine runs without them using a
# scikit-learn / numpy fallback so the platform is always demonstrable.
scikit-learn==1.5.0
xgboost==2.0.3
shap==0.45.1
lime==0.2.0.1
